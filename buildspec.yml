version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo Installing dependencies...
      - npm install

  build:
    commands:
      - echo Build phase started...
      - echo "No build step needed for this app"

artifacts:
  files:
    - '**/*'
